name: Test SDK functions

on: [push]

jobs:
  test:
    runs-on: macos-latest
    name: 🚀 Test SDK functions
    steps:
      - name: 📚 checkout
        uses: actions/checkout@v2.1.1
      - name: 🟢 node
        uses: actions/setup-node@v2.5.0
        with:
          node-version: 20.5.0
          registry-url: https://registry.npmjs.org
      - name: 📦 install dependencies
        run: npm install
      - name: Run tests
        run: npm test